<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="jp.ac.ehime_u.cite.udptest" android:versionCode="1"
	android:versionName="1.0">
	<uses-sdk android:minSdkVersion="7" />

	<uses-permission android:name="android.permission.INTERNET"></uses-permission>
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.BLUETOOTH"/>
	<uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
	<uses-permission android:name="android.permission.READ_PHONE_STATE"/>

	<application android:icon="@drawable/icon" android:label="@string/app_name" android:debuggable="true">
		<activity android:name=".AODV_Activity"
		    android:label="@string/app_name"
		    android:launchMode="singleTask" >
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
			<!-- 暗黙のIntent用フィルタ -->
			<intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="connect" />
			</intent-filter>
		</activity>
		
		<receiver android:name=".BroadcastReceiver"
		    android:exported="false" />
		
		<activity android:name=".RouteActivity"></activity>
		
		<service android:name=".AODV_Service">
			<intent-filter>
				<!-- バインドの対象となるサービスで提供するインターフェース -->
				<action android:name="jp.ac.ehime_u.cite.udptest.SendManager" />
				<!-- 特定のクラスを指定せずにサービスを指定するアクションコード -->
				<action android:name="jp.ac.ehime_u.cite.udptest.REMOTE_SERVICE" />
			</intent-filter>
		</service>
	</application>
</manifest>